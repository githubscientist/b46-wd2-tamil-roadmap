// function reducer(previousValue, currentValue) {
//     console.log(`previousValue: ${previousValue}, currentValue: ${currentValue}`);
//     return previousValue + currentValue;
// }

// let numbers = [1, 2, 3, 4, 5];

// console.log(numbers.reduce(reducer));

// let numbers = [1, 2, 3, 4, 5];

// console.log(numbers.reduce((p, c) => p * c));

// let words = ['this', 'is', 'an', 'example', 'sentence'];

// let sentence = words.reduce((pWord, cWord) => pWord + ' ' + cWord);

// console.log(sentence);

// function reducer(previousValue, currentValue) {
//     console.log(`previousValue: ${previousValue}, currentValue: ${currentValue}`);
//     return previousValue + currentValue;
// }

// let numbers = [1, 2, -3, 4, -5];

// console.log(numbers.reduce(reducer));


// let geekCoins = {
//     initialCoins: 100,
//     transactions: [20, 30, -1, -1, -1, -1, -1, 10, 300, -1, -1, -1]
// };

// // total geekcoins
// console.log(geekCoins.transactions.reduce((p, c) => p + c, geekCoins.initialCoins));

// let numbers = [1, 2, 3, 4, 5, 6];

// console.log(numbers.reduce((p, c) => {
//     if (c % 2 == 0) {
//         return p + c;
//     } else {
//         return p;
//     }
// }, 0));

